# Managed by chezmoi. Edit the source at ~/Documents/dotfiles/private_dot_config/starship.toml
# (do not edit this file directly).

# Shorten the hostname to its short name (cut at the first dot), so
# FQDNs like `host.example.com` render as `host`.
[hostname]
trim_at = '.'

# Hide the Google Cloud prompt segment.
[gcloud]
disabled = true
